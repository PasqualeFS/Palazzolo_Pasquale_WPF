// This is the JS file to go along with the Casting Screencast

/*
var stringVar = "6"; // Declaring the character 6 (not the number) as a variable.
var result = 7 + Number(stringVar); // This is attempting to add a number to a string.
console.log(result); // Printing the result to the console to verify.
*/

var areaCode = 407;
var firstPart = 555;
var lastPart = 1234;

var phoneNo = String(areaCode) + String(firstPart) + String(lastPart); // this will now concatenate three variables
console.log(phoneNo); // prints to the console